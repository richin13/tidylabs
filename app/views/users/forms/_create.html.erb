<%= simple_form_for @user, url: :new_collaborator do |f| %>
  <div class="row">
    <div class="col-xs-12 col-sm-6">
      <%= f.input :name, label: 'Nombre' %>
    </div>
    <div class="col-xs-12 col-sm-6">
      <%= f.input :lastname, label: 'Apellido(s)' %>
    </div>
  </div>
  <div class="row">
    <div class="col-xs-12 col-sm-6">
      <%= f.input :email, label: 'Correo electrónico' %>
    </div>
    <div class="col-xs-12 col-sm-6">
      <%= f.input :pin, label: 'PIN', as: :string %>
    </div>
  </div>
  <div class="row">
    <div class="col-xs-12">
      <div class="alert alert-info alert-dismissable" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
        <p><%= fa_icon('info') %> Los usuarios colaboradores no poseen credenciales de autenticación.
          La aplicación Tydilabs móvil solicita un PIN de ingreso.</p>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-xs-6">
      <%= f.submit 'Guardar', class: 'btn btn-primary' %>
    </div>
  </div>
<% end %>
